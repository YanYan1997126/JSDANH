function fc(){ 
    let b = document.querySelector('#a').value;
        c = parseInt(b);
       if (c%2 == 0) {
           window.alert(c + ' là số chẵn');
         
       }
       else {
        window.alert(c + ' là số lẻ');
       }
    }
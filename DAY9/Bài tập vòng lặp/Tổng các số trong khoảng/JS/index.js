let minNumber = parseInt(prompt('Enter the min value'));
let maxNumber = parseInt(prompt('Enter the max value '));
let sum = 0;
for(i=minNumber;i<=maxNumber;i++){
    sum+=i
} 
document.write("The sum of the range is "+ sum);

